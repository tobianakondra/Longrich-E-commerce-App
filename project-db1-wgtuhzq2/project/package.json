{
  "name": "modern-ecommerce-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
"scripts": {
  "dev": "vite",
  "build": "vite build && node scripts/post-build.js && node scripts/generate-seo.cjs && node scripts/prerender.mjs",
  "build:prod": "vite build --mode production && node scripts/post-build.js && node scripts/generate-seo.cjs && node scripts/prerender.mjs",
  "lint": "eslint .",
  "preview": "vite preview",
  "test:rules": "cd tests && npx jest security --runInBand"
}
}
